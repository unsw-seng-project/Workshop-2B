package accounts
import network._;

object Administrator {
	def penaliseUser ():Boolean = {
		var successful : Boolean = false;
		if (constants.PENALTYFARE <= (balance - constants.MINBALANCE)
				&& status == AccountStatus.Enabled) {
			balance -= constants.PENALTYFARE
			userEntryPoint = null
		}
		return successful
	}
}